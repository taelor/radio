<!DOCTYPE html>
%html
  %head
    %meta{ :content=>"text/html; charset=UTF-8", :"http-equiv" => "Content-Type"}
  %body
    %h1= @episode.to_s
    %i= "With #{@episode.guest}"
    %p= @episode.recording_description
    
    %h2 Upcoming Episodes
    - Episode.where(:recording_datetime.gt => @episode.recording_datetime).reverse.each do |episode|
      %p= "#{episode.recording_description} - #{episode.title}"
      
    = render "signature"