= render "show_navigation"

%h1
  = resource.to_s
%hr

%p= link_to("Email Schedule", send_email_episode_path(@episode, :email => "schedule") , :method => :post)

%p= link_to("Email Script", send_email_episode_path(@episode, :email => "script") , :method => :post)

%p= link_to("Needed Items", send_email_episode_path(@episode, :email => "needed_items") , :method => :post)